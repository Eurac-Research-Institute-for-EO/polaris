# Hello Nginx

This is a simple HTML page we have in the cluster to test app deployments. It is basically a Hello World page to see that the apps are being deployed by Argo CD. We use Nginx because it is simple to deploy, it is NOT used for anything.
